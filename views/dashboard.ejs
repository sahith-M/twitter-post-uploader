<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
    <h1>Dashboard</h1>
    <p>You are logged in. Here are your recent tweets:</p>
    <ul>
        <% if (recentTweets && recentTweets.length > 0) { %>
            <% recentTweets.forEach(tweet => { %>
                <li><%= tweet.text %> - <small><%= tweet.created_at %></small></li>
            <% }); %>
        <% } else { %>
            <li>No recent tweets found.</li>
        <% } %>
    </ul>

    <h2>Post a Tweet</h2>
    <form action="/post" method="POST" enctype="multipart/form-data">
        <label for="caption">Tweet Caption:</label><br>
        <textarea id="caption" name="caption" rows="4" cols="50" required></textarea><br><br>

        <label for="image">Attach an Image (optional):</label><br>
        <input type="file" id="image" name="image" accept="image/*"><br><br>

        <button type="submit">Post Tweet</button>
    </form>

    <h2>Schedule a Tweet</h2>
    <form action="/schedule" method="POST">
        <label for="schedule-caption">Tweet Caption:</label><br>
        <textarea id="schedule-caption" name="caption" rows="4" cols="50" required></textarea><br><br>

        <label for="schedule-time">Schedule Time (YYYY-MM-DD HH:mm:ss):</label><br>
        <input type="datetime-local" id="schedule-time" name="scheduleTime" required><br><br>

        <button type="submit">Schedule Tweet</button>
    </form>
</body>
</html>
